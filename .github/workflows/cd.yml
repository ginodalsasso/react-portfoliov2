name: CD - Deploy to VPS

# when this workflow should run
on:
  # Only when pushing to the main branch
  push:
    branches: [ main ]
  # Allows manual triggering from the GitHub interface
  workflow_dispatch:

permissions:
  contents: read

# Ensure only one deployment to production runs at a time
concurrency:
  group: deploy-production
  cancel-in-progress: true 

jobs:
  # Deploy job
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with: 
          persist-credentials: false  # Disable GitHub token to avoid permission issues
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build application
        run: npm run build

      - name: Deploy to OVH via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 22
          sftp_only: true
          local_path: './dist/*'
          remote_path: ${{ secrets.FTP_SERVER_DIR }}

      - name: Notify deployment success
        if: success()
        run: |
          echo "================================"
          echo "✅  Deployment succeeded !"
          echo "Date: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "Commit: ${{ github.event.head_commit.message }}"
          echo "================================"
      
      - name: Notify deployment failure
        if: failure()
        run: |
          echo "================================"
          echo "❌  Deployment failed !"
          echo "Date: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "Commit: ${{ github.event.head_commit.message }}"
          echo "================================"
          exit 1